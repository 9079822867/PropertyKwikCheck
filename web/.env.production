# Single-domain hosting: the SPA is served by the .NET app, so it calls same-origin "/api".
# (Leaving VITE_API_BASE_URL unset makes the Axios client use "/api".)
# To point a standalone SPA at a remote API instead, set:
# VITE_API_BASE_URL=https://property.kwikcheck.in/api
